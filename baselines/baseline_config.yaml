admin: SE # 실험자 이름
patience: 3
seed: 42
train:
  model_name: snunlp/KR-ELECTRA-discriminator
  batch_size: 32
  epoch: 100
  LR: 0.00001
  LossF: CosineEmbeddingLoss #MSELoss
  optim: AdamW
  # LR_scheduler : Step
  shuffle: True
  max_len: 100
select_clean:
  - remove_special_word
select_DA:
  - swap_sentence
  # - random_deletion
